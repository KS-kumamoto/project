# For local development (optional override, otherwise config proxy is used by default if empty)
# VITE_API_URL=http://localhost:8080

# When deploying to Vercel, you will set VITE_API_URL in their dashboard to your Render backend URL.
# Example:
# VITE_API_URL=https://my-spring-app.onrender.com
